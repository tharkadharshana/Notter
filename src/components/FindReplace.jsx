// CodeMirror's built-in search panel is rendered inside the editor container.
// This component is intentionally empty — the search panel is controlled via
// openSearchPanel() / closeSearchPanel() from @codemirror/search.
export default function FindReplace() { return null; }
